angular.module('talkos')
    .service('UserService', ['$scope',
        function ($scope) {
            return {

                save: function(user) {

                }

            };
        }]);
