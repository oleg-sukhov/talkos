apply plugin: 'war'

war {
    baseName = 'talkos'
    version =  '0.0.1'
}

dependencies {
    compile("javax.servlet:javax.servlet-api:$servlet")
    compile("org.springframework:spring-webmvc:$springMvc")
    compile("org.springframework.security:spring-security-web:$springSecurity")
    compile("org.springframework.security:spring-security-config:$springSecurity")
}
